<%= form_for(@archivo_tarea) do |f| %>
  <% if @archivo_tarea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@archivo_tarea.errors.count, "error") %> prohibited this archivo_tarea from being saved:</h2>

      <ul>
      <% @archivo_tarea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :archivo_id %><br>
    <%= f.number_field :archivo_id %>
  </div>
  <div class="field">
    <%= f.label :tarea_id %><br>
    <%= f.number_field :tarea_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
