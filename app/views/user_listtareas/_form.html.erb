<%= form_for(@user_listtarea) do |f| %>
  <% if @user_listtarea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_listtarea.errors.count, "error") %> prohibited this user_listtarea from being saved:</h2>

      <ul>
      <% @user_listtarea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :listtarea_id %><br>
    <%= f.number_field :listtarea_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
