<%= form_for(@user_hito) do |f| %>
  <% if @user_hito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_hito.errors.count, "error") %> prohibited this user_hito from being saved:</h2>

      <ul>
      <% @user_hito.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :hito_id %><br>
    <%= f.number_field :hito_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
