<%= form_for(@archivo_hito) do |f| %>
  <% if @archivo_hito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@archivo_hito.errors.count, "error") %> prohibited this archivo_hito from being saved:</h2>

      <ul>
      <% @archivo_hito.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :archivo_id %><br>
    <%= f.number_field :archivo_id %>
  </div>
  <div class="field">
    <%= f.label :hito_id %><br>
    <%= f.number_field :hito_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
