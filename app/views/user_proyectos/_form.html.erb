<%= form_for(@user_proyecto) do |f| %>
  <% if @user_proyecto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_proyecto.errors.count, "error") %> prohibited this user_proyecto from being saved:</h2>

      <ul>
      <% @user_proyecto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :proyecto_id %><br>
    <%= f.number_field :proyecto_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
