<%= form_for(@user_tarea) do |f| %>
  <% if @user_tarea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_tarea.errors.count, "error") %> prohibited this user_tarea from being saved:</h2>

      <ul>
      <% @user_tarea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :tarea_id %><br>
    <%= f.number_field :tarea_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
